import { QuestionPipe } from './question.pipe';

/*
describe('QuestionPipe', () => {
  it('create an instance', () => {
    const pipe = new QuestionPipe();
    expect(pipe).toBeTruthy();
  });
});
*/